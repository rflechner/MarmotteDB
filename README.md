# MarmotteDB

Dev in progress

L’index trié par fragments est documenté dans
[docs/sorted-index-table.md](docs/sorted-index-table.md), avec son format disque,
son API et ses limites de persistance.

Exécuter les tests depuis la racine du dépôt :

```powershell
cargo test --offline --manifest-path marmotte-server/Cargo.toml
```
